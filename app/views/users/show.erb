<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Welcome,</h1>
      <h2><%= @user.username %></h2>
      <% @user.tweets.each_with_index do |tweet, index|%>
        <%= tweet.content %><br>
        <%end%>
  </body>
</html>
