<! DOCTYPE html>
<html>
  <head>
  </head>
  <body>
    <nav>
    <% if logged_in? %>
      <a href="/tweets">Welcome, <%=current_user.username%></a>
      <a href="/logout">Log Out</a>
    <% else %>
      <a href="/signup">Sign Up</a>
      <a href="/login">Log In</a>
    <% end %>
    </nav>
    <%= yield %>
  </body>
</html>
